{
  "agent_type": "claude-code",
  "harness_version": 2,
  "notes": "기본값. openclaw/codex로 변경 시 /project:host 안내 메시지가 안내합니다. backup 설정은 /project:backup-sync init 으로 추가 가능 (옵셔널). atlassian 설정(사이트·cloudId·스페이스)은 `python3 .claude/bin/atlassian_map.py init` 으로 추가합니다 — 템플릿에는 넣지 않습니다 (머신·조직마다 다름)."
}

# verify-loop 상태 디렉토리 (claude.loope, Loop 2)

`verify_loop.py` 의 런타임 상태:
- `<Feature>.json` — feature 별 검증 루프 (rubric·grader 판정 이력·재시도·에스컬레이션)

런타임 산출물(<F>.json)은 gitignore. `.gitkeep` + 이 README 만 커밋 (ADR-014).
